# Model prices and vision token formulas.
#
# Every entry carries its own `last_verified` date. complydoc prints that date in
# every report and warns when it is older than `staleness_warn_days`. An
# entry with `last_verified: null` is treated as never verified and warned about
# on every run until you check it and set the date yourself.
#
# Entries with `enabled: false` and null prices are templates. The token formula
# shape is filled in; the price is not, because it was not sourced when this file
# was written. Fill in the price, set last_verified, flip enabled to true.
# complydoc will not invent a price for you.

schema_version: 1
staleness_warn_days: 90

# Processing time is not estimated by default. Prefill throughput depends on your
# account, region and load, complydoc cannot measure it offline, and it will not
# invent one. Add `input_tokens_per_second:` to a model from your own benchmark
# and the report will estimate how long a document takes on it.

currency:
  # Costs are computed in USD because that is the currency providers publish in.
  # For GBP totals, set a rate you trust plus the date you checked it, and change
  # report_in to GBP. Until then complydoc reports USD and says so in every report.
  report_in: USD
  usd_to_gbp:
    rate: null
    last_verified: null
    source_url: null

vision_formulas:
  width_height_area:
    kind: width_height
    divisor: 750
    cap_tokens: 1600
    notes: >
      tokens ~= (width_px * height_px) / 750, capped. Used by Anthropic models.
      Images above the cap are downscaled by the provider before counting.

  tiled_512:
    kind: tiled
    base_tokens: 85
    tile_tokens: 170
    tile_px: 512
    max_short_side_px: 768
    max_long_side_px: 2000
    notes: >
      Image is resized to fit the long/short side limits, then split into tile_px
      squares. Cost is base_tokens plus tile_tokens per tile. Shape only - confirm
      the constants against your provider's current documentation.

  flat_then_tiled:
    kind: flat_per_image
    tokens_per_image: 258
    tile_above_px: 384
    tile_tokens: 258
    notes: >
      Flat count per image below tile_above_px on both sides; larger images are
      cropped into tiles each costing tile_tokens. Shape only - confirm constants.

resolution_presets:
  low:
    long_edge_px: 768
  medium:
    long_edge_px: 1536
  high:
    long_edge_px: 2048

models:
  - id: claude-opus-5-5
    provider: anthropic
    display_name: Claude Opus 5.5
    enabled: true
    input_per_mtok_usd: 4.00
    output_per_mtok_usd: 20.00
    batch_input_per_mtok_usd: 2.00
    supports_vision: true
    vision_formula: width_height_area
    tokenizer:
      encoding: o200k_base
      fidelity: approximate
      approximate_ratio: 1.0
      approximation_note: >
        Anthropic does not publish an offline tokenizer, so text token counts are
        approximated with the o200k_base encoding.
    last_verified: 2026-09-25
    source_url: https://platform.claude.com/docs/en/about-claude/pricing

  - id: claude-opus-5
    provider: anthropic
    display_name: Claude Opus 5
    enabled: true
    input_per_mtok_usd: 5.00
    output_per_mtok_usd: 25.00
    batch_input_per_mtok_usd: 2.50
    supports_vision: true
    vision_formula: width_height_area
    tokenizer:
      encoding: o200k_base
      fidelity: approximate
      approximate_ratio: 1.0
      approximation_note: >
        Anthropic does not publish an offline tokenizer, so text token counts are
        approximated with the o200k_base encoding.
    last_verified: 2026-09-25
    source_url: https://platform.claude.com/docs/en/about-claude/pricing

  - id: claude-sonnet-5
    provider: anthropic
    display_name: Claude Sonnet 5
    enabled: true
    input_per_mtok_usd: 2.00
    output_per_mtok_usd: 10.00
    batch_input_per_mtok_usd: 1.00
    supports_vision: true
    vision_formula: width_height_area
    tokenizer:
      encoding: o200k_base
      fidelity: approximate
      approximate_ratio: 1.0
      approximation_note: >
        Anthropic does not publish an offline tokenizer, so text token counts are
        approximated with the o200k_base encoding.
    last_verified: 2026-09-25
    source_url: https://platform.claude.com/docs/en/about-claude/pricing

  - id: claude-haiku-4-5
    provider: anthropic
    display_name: Claude Haiku 4.5
    enabled: true
    input_per_mtok_usd: 1.00
    output_per_mtok_usd: 5.00
    batch_input_per_mtok_usd: 0.50
    supports_vision: true
    vision_formula: width_height_area
    tokenizer:
      encoding: o200k_base
      fidelity: approximate
      approximate_ratio: 1.0
      approximation_note: >
        Anthropic does not publish an offline tokenizer, so text token counts are
        approximated with the o200k_base encoding.
    last_verified: 2026-09-25
    source_url: https://platform.claude.com/docs/en/about-claude/pricing

  # Imported from the litellm price table on 2026-09-09.
  # Prices are per million tokens, USD. Check prices before relying on them.
  # Only the import date is recorded.

  - id: deepseek-v4-flash-vision-exp
    provider: deepseek
    display_name: "DeepSeek V4 Flash Vision Exp"
    enabled: true
    input_per_mtok_usd: 0.44
    output_per_mtok_usd: 1.32
    supports_vision: true
    vision_formula: tiled_512
    tokenizer:
      encoding: o200k_base
      fidelity: approximate
      approximate_ratio: 1.0
      approximation_note: >
        deepseek does not publish an offline tokenizer; token counts are
        approximated with the o200k_base encoding.
    last_verified: 2026-09-09
    source_url: https://github.com/BerriAI/litellm/blob/main/model_prices_and_context_window.json
    notes: >
      Token prices are from the litellm table. The vision token formula is
      assumed to follow the OpenAI tiling convention because this provider
      serves an OpenAI-compatible API; confirm it before relying on the
      vision figures.

  - id: gemini/gemini-3.7-flash
    provider: gemini
    display_name: "Gemini 3.7 Flash"
    enabled: true
    input_per_mtok_usd: 0.75
    output_per_mtok_usd: 3.75
    supports_vision: true
    vision_formula: flat_then_tiled
    tokenizer:
      encoding: o200k_base
      fidelity: approximate
      approximate_ratio: 1.0
      approximation_note: >
        gemini does not publish an offline tokenizer; token counts are
        approximated with the o200k_base encoding.
    last_verified: 2026-09-09
    source_url: https://github.com/BerriAI/litellm/blob/main/model_prices_and_context_window.json

  - id: gemini/gemini-3.1-pro-preview
    provider: gemini
    display_name: "Gemini 3.1 Pro Preview"
    enabled: true
    input_per_mtok_usd: 2.0
    output_per_mtok_usd: 12.0
    supports_vision: true
    vision_formula: flat_then_tiled
    tokenizer:
      encoding: o200k_base
      fidelity: approximate
      approximate_ratio: 1.0
      approximation_note: >
        gemini does not publish an offline tokenizer; token counts are
        approximated with the o200k_base encoding.
    last_verified: 2026-09-09
    source_url: https://github.com/BerriAI/litellm/blob/main/model_prices_and_context_window.json

  - id: moonshot/kimi-k3
    provider: moonshot
    display_name: "Kimi K3"
    enabled: true
    input_per_mtok_usd: 3.0
    output_per_mtok_usd: 15.0
    supports_vision: true
    vision_formula: tiled_512
    tokenizer:
      encoding: o200k_base
      fidelity: approximate
      approximate_ratio: 1.0
      approximation_note: >
        moonshot does not publish an offline tokenizer; token counts are
        approximated with the o200k_base encoding.
    last_verified: 2026-09-09
    source_url: https://github.com/BerriAI/litellm/blob/main/model_prices_and_context_window.json
    notes: >
      Token prices are from the litellm table. The vision token formula is
      assumed to follow the OpenAI tiling convention because this provider
      serves an OpenAI-compatible API; confirm it before relying on the
      vision figures.

  - id: gpt-5-mini
    provider: openai
    display_name: "GPT-5 Mini"
    enabled: true
    input_per_mtok_usd: 0.25
    output_per_mtok_usd: 2.0
    supports_vision: true
    vision_formula: tiled_512
    tokenizer:
      encoding: o200k_base
      fidelity: exact
    last_verified: 2026-09-09
    source_url: https://github.com/BerriAI/litellm/blob/main/model_prices_and_context_window.json

  - id: gpt-5.2
    provider: openai
    display_name: "GPT-5.2"
    enabled: true
    input_per_mtok_usd: 1.75
    output_per_mtok_usd: 14.0
    supports_vision: true
    vision_formula: tiled_512
    tokenizer:
      encoding: o200k_base
      fidelity: exact
    last_verified: 2026-09-09
    source_url: https://github.com/BerriAI/litellm/blob/main/model_prices_and_context_window.json

  - id: zai/glm-5.3-flash
    provider: zai
    display_name: "GLM-5.3-Flash"
    enabled: true
    input_per_mtok_usd: 0.15
    output_per_mtok_usd: 0.5
    supports_vision: true
    vision_formula: tiled_512
    tokenizer:
      encoding: o200k_base
      fidelity: approximate
      approximate_ratio: 1.0
      approximation_note: >
        zai does not publish an offline tokenizer; token counts are
        approximated with the o200k_base encoding.
    last_verified: 2026-09-09
    source_url: https://github.com/BerriAI/litellm/blob/main/model_prices_and_context_window.json
    notes: >
      Token prices are from the litellm table. The vision token formula is
      assumed to follow the OpenAI tiling convention because this provider
      serves an OpenAI-compatible API; confirm it before relying on the
      vision figures.

# Per-page prices for the presets in complydoc.loaders.parsers. A null price is reported
# as not estimated. None of these has been checked against the provider's own
# pricing page, so every report that uses one says so.
parsers:
  docling:
    display_name: Docling (local)
    usd_per_1000_pages: 0.0
    notes: Runs locally; machine time is not priced.
  unstructured_api:
    display_name: Unstructured API
    usd_per_1000_pages: null
    notes: Set this from your Unstructured plan.
  llamaparse_fast:
    display_name: LlamaParse (Fast)
    usd_per_1000_pages: 1.25
    last_verified: null
    source_url: https://developers.llamaindex.ai/python/cloud/general/pricing/
    notes: 1 credit per page, $1.25 per 1,000 credits on paid plans.
  llamaparse_cost_effective:
    display_name: LlamaParse (Cost Effective)
    usd_per_1000_pages: 3.75
    last_verified: null
    source_url: https://developers.llamaindex.ai/python/cloud/general/pricing/
    notes: 3 credits per page, $1.25 per 1,000 credits on paid plans.
  llamaparse_agentic:
    display_name: LlamaParse (Agentic)
    usd_per_1000_pages: 12.5
    last_verified: null
    source_url: https://developers.llamaindex.ai/python/cloud/general/pricing/
    notes: 10 credits per page, $1.25 per 1,000 credits on paid plans.
  llamaparse_agentic_plus:
    display_name: LlamaParse (Agentic Plus)
    usd_per_1000_pages: 56.25
    last_verified: null
    source_url: https://developers.llamaindex.ai/python/cloud/general/pricing/
    notes: 45 credits per page, $1.25 per 1,000 credits on paid plans.
  azure_read:
    display_name: Azure Document Intelligence (Read)
    usd_per_1000_pages: 1.5
    last_verified: null
    source_url: https://azure.microsoft.com/pricing/details/ai-document-intelligence/
    notes: Pay-as-you-go S0 tier.
  azure_layout:
    display_name: Azure Document Intelligence (Layout)
    usd_per_1000_pages: 10.0
    last_verified: null
    source_url: https://azure.microsoft.com/pricing/details/ai-document-intelligence/
    notes: Pay-as-you-go S0 tier.
